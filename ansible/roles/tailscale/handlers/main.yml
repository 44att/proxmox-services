---
- name: Reboot LXC
  reboot:

- name: Restart systemd-resolved
  systemd:
    name: systemd-resolved
    state: restarted
    daemon_reload: true

- name: Restart Tailscale
  systemd:
    name: tailscaled
    state: restarted
    daemon_reload: true
